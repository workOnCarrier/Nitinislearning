## assert isinstance(value, int), `Expected int value`

* enforces the type checking on the value
* if assertion fails, the error message is displayed

## decorator @clasmethod 

* invoked on class name instead of object name
* e.g.
* class Contract:
*   @classmethod:
*   def check(cls, value):
*       pass